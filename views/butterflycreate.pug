extends layout

block content
  h1= title

  form(id='butterflysForm', action="/resource/butterfly", method="POST")
    div.form-group
        label.butterflyAttr(for='Size') Size
        input#butterflystype(type='text', name='Size')
    div.form-group
        label.butterflyAttr(for='color') color
        input#size(type='text', name='color')
    div.form-group
        label.butterflyAttr(for='price') price
        input#cost(type='number', name='price')
    input(type="submit")

  script.
        document.forms['butterflysForm'].addEventListener('submit', (event) => {
            event.preventDefault();
            // TODO do something here to show user that form is being submitted
            fetch(event.target.action, {
                method: 'POST',
                body: new URLSearchParams(new FormData(event.target)) // event.target is the form
            }).then((resp) => {
                return resp.json(); // or resp.text() or whatever the server sends
            }).then((body) => {

           alert("Save succeded")
                // TODO handle body
            }).catch((error) => {
                // TODO handle error
                alert("Save failed " + error )
            });
        });